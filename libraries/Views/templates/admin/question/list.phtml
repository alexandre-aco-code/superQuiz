<!-- list des tags -->
<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center my-4">
    <a class="btn btn-primary" href="<?= $tplVars['WWW_URL']; ?>index.php?controller=Admin\<?= $tplVars['crud']; ?>&task=newForm"><i class="fa fa-plus"></i> Ajouter une Question</a>
</div>
<div class="table-responsive">
    <table class="table table-bordered table-striped">
        <thead class="thead-dark">
            <tr>
                <th>Id</th>
                <th>Topic</th>
                <th>Question</th>
                <th>Réponses</th>
                <th>Bonne réponse</th>
                <th>URL Image</th>
                <th>Modifier</th>
                <th>Supprimer</th>
            </tr>
        </thead>
        <tbody>
            <!-- boucle sur les tags -->
            <?php foreach ($tplVars['list'] as $item) : ?>
                <tr>
                    <!-- <td><?= var_dump($item); ?></td> -->
                    <td><?= htmlspecialchars($item['Id']); ?></td>
                    <td><?= htmlspecialchars($item['Name']); ?></td>
                    <td><?= htmlspecialchars($item['Question']); ?></td>
                    <td><?= htmlspecialchars($item['Answers']); ?></td>
                    <td><?= htmlspecialchars($item['IndexGoodAnswer']); ?></td>
                    <td><?= htmlspecialchars($item['Image']); ?></td>
                    <td class="text-right">
                        <a class="btn btn-sm btn-outline-secondary" href="<?= $tplVars['WWW_URL']; ?>index.php?controller=Admin\<?= $tplVars['crud']; ?>&task=editForm&id=<?= intval($item['Id']); ?>"> Modifier</a>
                    </td>
                    <td class="text-right">
                        <a class="btn btn-sm btn-outline-danger" href="<?= $tplVars['WWW_URL']; ?>index.php?controller=Admin\<?= $tplVars['crud']; ?>&task=delete&id=<?= intval($item['Id']); ?>"><i class="fa fa-archive"></i> Supprimer</a>
                    </td>
                </tr>
            <?php endforeach; ?>
            <!-- boucle sur les tags -->
        </tbody>
    </table>
</div>
<!-- list des tags -->